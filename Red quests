local plr = game.Players.LocalPlayer
	local quest1 = game.Workspace.FriendlyNPCs["Quest Giver"]
	local quest2 = game.Workspace.FriendlyNPCs["Quest Giver"]
	local cf1
	local cf2
	if quest1:FindFirstChild("HumanoidRootPart") then
		cf1 = quest1.HumanoidRootPart.CFrame
	end
	if quest2:FindFirstChild("HumanoidRootPart") then
		cf2 = quest2.HumanoidRootPart.CFrame
	end
	local cloneQuest1 = quest1:Clone()
	local cloneQuest2 = quest2:Clone()
	local ChatStart = plr.Backpack.ServerTraits.ChatStart
	ChatStart:FireServer(workspace.FriendlyNPCs["Bulma"])
	wait(0.5)
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "k",
	})
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "k",
	})
	wait(0.5)
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "Yes",
	})
	wait(0.5)
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "k",
	})
	wait(0.7)
	local ChatStart = plr.Backpack.ServerTraits.ChatStart
	ChatStart:FireServer(workspace.FriendlyNPCs["Spaceship"])
	wait(0.5)
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "k",
	})
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "k",
	})
	wait(0.5)
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "No",
	})
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "k",
	})
	wait(0.7)
	--/////////////////////////////////////////////////////
	wait()
	local ChatStart = plr.Backpack.ServerTraits.ChatStart
	ChatStart:FireServer(workspace.FriendlyNPCs["Trunks [Future]"])
	wait(0.5)
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "k",
	})
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "k",
	})
	wait(0.5)
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "Yes",
	})
	wait(0.5)
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "k",
	})
	wait(0.7)
	local ChatStart = plr.Backpack.ServerTraits.ChatStart
	ChatStart:FireServer(workspace.FriendlyNPCs["TimeMachine"])
	wait(0.5)
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "k",
	})
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "k",
	})
	wait(0.5)
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "No",
	})
	wait(0.7)
	local ChatStart = plr.Backpack.ServerTraits.ChatStart
	ChatStart:FireServer(workspace.FriendlyNPCs["Quest Giver"])
	wait(0.5)
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "k",
	})
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "k",
	})
	wait(0.5)
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "Yes",
	})
	wait(0.5)
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "k",
	})
	wait(0.7)
	wait()
	local ChatStart = plr.Backpack.ServerTraits.ChatStart
	ChatStart:FireServer(workspace.FriendlyNPCs["NamekianShip"])
	wait(0.5)
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "k",
	})
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "k",
	})
	wait(0.5)
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "No",
	})
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "k",
	})
	wait(0.7)
	local ChatStart = plr.Backpack.ServerTraits.ChatStart
	ChatStart:FireServer(workspace.FriendlyNPCs["Elder Kai"])
	wait(0.5)
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "k",
	})
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "k",
	})
	wait(0.5)
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "k",
	})
	wait(0.5)
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "Yes",
	})
	wait(0.5)
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "k",
	})
	wait(2.5)
	local ChatStart = plr.Backpack.ServerTraits.ChatStart
	ChatStart:FireServer(workspace.FriendlyNPCs["Korin"])
	wait(0.5)
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "k",
	})
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "k",
	})
	wait(0.5)
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "k",
	})
	wait(0.5)
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "k",
	})
	wait(0.5)
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "DRINK",
	})
	wait(0.5)
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "k",
	})
	cloneQuest1.Parent = workspace.FriendlyNPCs
	cloneQuest2.Parent = workspace.FriendlyNPCs
	if cloneQuest1:FindFirstChild("HumanoidRootPart") then
		cloneQuest1.HumanoidRootPart.CFrame = cf1
	end
	if cloneQuest2:FindFirstChild("HumanoidRootPart") then
		cloneQuest2.HumanoidRootPart.CFrame = cf2
	end
