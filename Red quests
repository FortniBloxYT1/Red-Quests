for _, Object in ipairs(workspace:GetDescendants()) do
    if Object.Name == "Chair" then
        Object:Destroy()
    end
end

local TS = game:GetService("TweenService")
local plr = game.Players.LocalPlayer
	game.Workspace.FriendlyNPCs["Quest Giver"].Name = "Finishedd"
	game.Workspace.FriendlyNPCs["Quest Giver"].Name = "Finishedd"
	getgenv().DoingRedQuests = true
    local Tween = TS:Create(plr.Character.HumanoidRootPart, TweenInfo.new((plr.Character.HumanoidRootPart.Position - workspace.FriendlyNPCs["Bulma"].WorldPivot.Position).magnitude / 700), {CFrame = workspace.FriendlyNPCs["Bulma"].WorldPivot})
    Tween:Play()
    Tween.Completed:Wait()
    task.wait(0.1)
	local ChatStart = plr.Backpack.ServerTraits.ChatStart
	ChatStart:FireServer(workspace.FriendlyNPCs["Bulma"])
	wait(0.5)
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "k",
	})
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "k",
	})
	wait(0.5)
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "Yes",
	})
	wait(0.5)
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "k",
	})
	wait(0.7)
    local Tween = TS:Create(plr.Character.HumanoidRootPart, TweenInfo.new((plr.Character.HumanoidRootPart.Position - workspace.FriendlyNPCs["Spaceship"].WorldPivot.Position).magnitude / 700), {CFrame = workspace.FriendlyNPCs["Spaceship"].WorldPivot})
    Tween:Play()
    Tween.Completed:Wait()
    task.wait(0.1)
	local ChatStart = plr.Backpack.ServerTraits.ChatStart
	ChatStart:FireServer(workspace.FriendlyNPCs["Spaceship"])
	wait(0.5)
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "k",
	})
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "k",
	})
	wait(0.5)
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "No",
	})
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "k",
	})
	wait(0.7)
	--/////////////////////////////////////////////////////
	wait()
    local Tween = TS:Create(plr.Character.HumanoidRootPart, TweenInfo.new((plr.Character.HumanoidRootPart.Position - workspace.FriendlyNPCs["Trunks [Future]"].WorldPivot.Position).magnitude / 700), {CFrame = workspace.FriendlyNPCs["Trunks [Future]"].WorldPivot})
    Tween:Play()
    Tween.Completed:Wait()
    task.wait(0.1)
	local ChatStart = plr.Backpack.ServerTraits.ChatStart
	ChatStart:FireServer(workspace.FriendlyNPCs["Trunks [Future]"])
	wait(0.5)
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "k",
	})
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "k",
	})
	wait(0.5)
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "Yes",
	})
	wait(0.5)
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "k",
	})
	wait(0.7)
    local Tween = TS:Create(plr.Character.HumanoidRootPart, TweenInfo.new((plr.Character.HumanoidRootPart.Position - workspace.FriendlyNPCs["TimeMachine"].WorldPivot.Position).magnitude / 700), {CFrame = workspace.FriendlyNPCs["TimeMachine"].WorldPivot})
    Tween:Play()
    Tween.Completed:Wait()
    task.wait(0.1)
	local ChatStart = plr.Backpack.ServerTraits.ChatStart
	ChatStart:FireServer(workspace.FriendlyNPCs["TimeMachine"])
	wait(0.5)
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "k",
	})
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "k",
	})
	wait(0.5)
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "No",
	})
	wait(0.7)
    local Tween = TS:Create(plr.Character.HumanoidRootPart, TweenInfo.new((plr.Character.HumanoidRootPart.Position - workspace.FriendlyNPCs["Quest Giver"].WorldPivot.Position).magnitude / 700), {CFrame = workspace.FriendlyNPCs["Quest Giver"].WorldPivot})
    Tween:Play()
    Tween.Completed:Wait()
    task.wait(0.1)
	local ChatStart = plr.Backpack.ServerTraits.ChatStart
	ChatStart:FireServer(workspace.FriendlyNPCs["Quest Giver"])
	wait(0.5)
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "k",
	})
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "k",
	})
	wait(0.5)
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "Yes",
	})
	wait(0.5)
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "k",
	})
	wait(0.7)
	wait()
    local Tween = TS:Create(plr.Character.HumanoidRootPart, TweenInfo.new((plr.Character.HumanoidRootPart.Position - workspace.FriendlyNPCs["NamekianShip"].WorldPivot.Position).magnitude / 700), {CFrame = workspace.FriendlyNPCs["NamekianShip"].WorldPivot})
    Tween:Play()
    Tween.Completed:Wait()
    task.wait(0.1)
	local ChatStart = plr.Backpack.ServerTraits.ChatStart
	ChatStart:FireServer(workspace.FriendlyNPCs["NamekianShip"])
	wait(0.5)
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "k",
	})
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "k",
	})
	wait(0.5)
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "No",
	})
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "k",
	})
	wait(0.7)
    local Tween = TS:Create(plr.Character.HumanoidRootPart, TweenInfo.new((plr.Character.HumanoidRootPart.Position - workspace.FriendlyNPCs["Elder Kai"].WorldPivot.Position).magnitude / 700), {CFrame = workspace.FriendlyNPCs["Elder Kai"].WorldPivot})
    Tween:Play()
    Tween.Completed:Wait()
    task.wait(0.1)
	local ChatStart = plr.Backpack.ServerTraits.ChatStart
	ChatStart:FireServer(workspace.FriendlyNPCs["Elder Kai"])
	wait(0.5)
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "k",
	})
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "k",
	})
	wait(0.5)
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "k",
	})
	wait(0.5)
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "Yes",
	})
	wait(0.5)
	local ChatAdvance = plr.Backpack.ServerTraits.ChatAdvance
	ChatAdvance:FireServer({
		[1] = "k",
	})
	wait(2)
	if workspace.FriendlyNPCs:FindFirstChild("Finishedd") then
		workspace.FriendlyNPCs.Finishedd.Name = "Quest Giver"
	end
	if workspace.FriendlyNPCs:FindFirstChild("Finishedd") then
		workspace.FriendlyNPCs.Finishedd.Name = "Quest Giver"
	end

task.wait(0.5)
local Tween = TS:Create(plr.Character.HumanoidRootPart, TweenInfo.new((plr.Character.HumanoidRootPart.Position - workspace.FriendlyNPCs["Korin"].WorldPivot.Position).magnitude / 700), {CFrame = workspace.FriendlyNPCs["Korin"].WorldPivot})
Tween:Play()
Tween.Completed:Wait()
task.wait(0.1)
plr.Backpack.ServerTraits.ChatStart:FireServer(workspace.FriendlyNPCs.Korin.Chat.Chat)
task.wait(0.5)
plr.Backpack.ServerTraits.ChatAdvance:FireServer({"k"})
task.wait(0.5)
plr.Backpack.ServerTraits.ChatAdvance:FireServer({"k"})
task.wait(0.5)
plr.Backpack.ServerTraits.ChatAdvance:FireServer({"DRINK"})
getgenv().DoingRedQuests = false
